# ⚠️ DESABILITADO PARA RAILWAY!
# Este arquivo é APENAS para desenvolvimento local com Laravel Sail
# O Railway usa o Dockerfile na raiz do projeto
# 
# Conteúdo original em: compose.yaml.bak
# Para desenvolvimento local: docker-compose -f docker-compose.dev.yml up -d
